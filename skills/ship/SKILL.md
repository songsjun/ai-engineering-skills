---
name: ship
description: Pre-deployment checklist and deployment process. Use before deploying to staging or production. Triggers: deploy, 部署, 发布, ship, go to production, 上线, 准备发布, release, push to prod. Blocks common deployment failures through systematic verification.
---

# Ship（发布部署）

部署前先验证，上线前先定好回滚方案。

## 权限边界（核心设计）

本 skill 严格区分两类操作：

| 类型 | 定义 | 执行者 |
|---|---|---|
| **Agent 自主执行** | 静态分析、CI 监控、配置扫描、代码检查 | Agent 直接完成 |
| **人工决策点** | 触发生产部署、执行回滚、发送对外通知 | 必须人工确认 |

**Agent 在任何人工决策点前必须明确停止，输出所需决策内容，等待确认。**
这不是工具限制，而是设计原则：生产部署对外部系统产生不可逆影响，必须人工确认。

## 适用范围

使用此 skill：
- 生产环境
- 用户可见功能的 staging 环境
- 包含真实数据的任何环境

跳过：本地开发、无用户的个人项目。

---

## Gate 1：代码质量

**Agent 执行：**
- [ ] 代码中没有未完成实现（TODO、FIXME、占位代码）—— Agent 扫描并报告
- [ ] 源代码中没有硬编码的 secret、密码或 API key —— Agent 扫描并报告

**人工确认（Agent 展示状态后等待确认）：**
- [ ] CI 流水线为绿色（最重要的单一 gate）
- [ ] 代码审查已批准（团队项目）
- [ ] 没有未提交的更改混入此次部署

---

## Gate 2：配置

**Agent 执行：**
- [ ] 扫描代码库确认无硬编码密钥 —— Agent 执行
- [ ] 读取配置文件确认环境变量引用完整 —— Agent 执行

**人工确认：**
- [ ] 目标环境的环境变量已设置并已验证
- [ ] 外部服务端点指向正确的环境

---

## Gate 3：数据

**人工决策点（Agent 呈现以下清单，等待人工确认每项）：**

```md
## Data Gate 确认

请确认以下各项后，回复"Gate 3 confirmed"继续：

- [ ] 数据库 migration（如果有）已经过审查
- [ ] Migration 可以回滚，OR 存在数据备份，OR 已在类生产数据集上测试

Migration 问题是生产事故的常见原因。不确定时，先在非生产环境用生产级别的数据量测试。
```

---

## Gate 4：回滚

**人工决策点（Agent 引导填写，确认后才继续）：**

```md
## 回滚计划（部署前必须填写）

部署工具/方式：[如 Kubernetes rollout / Heroku / 手动 ssh]
预计回滚时间：[填写实际值]
回滚触发条件：如果 [错误率/关键指标] 在部署后 [时间窗口] 内超过 [阈值]
回滚步骤：[具体命令或操作]
```

如果回滚时间未知：**现在测试一次回滚比上线后发现不会更安全。**

---

## Gate 5：可观测性

**人工确认：**
- [ ] 关键指标正在被监控（错误率、延迟、相关业务指标）
- [ ] 异常告警已配置，知道谁会收到告警
- [ ] 出问题时知道在哪里看日志

---

## Gate 6：沟通（团队项目）

**人工执行：**
- [ ] 相关团队成员知道此次部署
- [ ] Changelog 或发布说明已撰写
- [ ] 如有用户可见变化，支持/客户团队已收到通知

---

## 部署触发（人工决策点）

所有 Gate 通过后，Agent 输出：

```md
## 部署就绪确认

所有 Gate 已通过：
- Gate 1 代码质量：✓
- Gate 2 配置：✓
- Gate 3 数据：✓（人工确认）
- Gate 4 回滚计划：✓（人工确认）
- Gate 5 可观测性：✓
- Gate 6 沟通：✓（适用时）

**需要人工触发部署。**
请确认后执行部署，并告知 Agent 部署完成时间，以便进行部署后验证。
```

---

## 部署后验证

人工触发部署并告知完成后，Agent 执行可自动化的验证：

- 读取健康检查端点（如可访问）
- 检查日志中是否有立即出现的错误
- 报告验证结果

**监控时长：** 根据系统风险等级由人工定义（高风险系统数小时，内部工具可能 5 分钟）。

---

## 如果出了问题

1. 不要在压力下不理解问题就试图"往前修"
2. 检查回滚触发条件——如果达到，**立即回滚**（人工执行）
3. 回滚前先通知团队
4. 回滚后，进入 debug skill

---

## 事后复盘

任何需要回滚或造成用户影响的部署：

```md
## 事后复盘：[简短标题]

**日期：** YYYY-MM-DD
**影响：** [受影响的用户数量/功能/时长]

### 事件时间线
[关键事件的时间戳列表]

### 根本原因
[不是"人为错误"——是什么系统或流程条件使这个错误成为可能？]

### 为何未能更早发现
[监控盲区、测试覆盖缺失等]

### 行动项
- [ ] [具体行动] — 负责人：[姓名] — 截止：[日期]
```

> **保持无指责。** 目标是系统改进，不是追责。
